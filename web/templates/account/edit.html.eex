<h2>Edit account</h2>

<%= render "form.html", changeset: @changeset,
                        action: account_path(@conn, :update, contacts: true) %>

<%= link "Back", to: account_path(@conn, :show) %>
