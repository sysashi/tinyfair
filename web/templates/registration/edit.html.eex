<h2>Edit registration</h2>

<%= render "form.html", changeset: @changeset,
                        action: registration_path(@conn, :update, @registration) %>

<%= link "Back", to: registration_path(@conn, :index) %>
